build
mx.scratch

# IDE / tool state (machine-specific, not useful to others)
.settings/
.vscode/*
!.vscode/launch.json
mx.scratch/
*.map
*.su

__pycache__/
*.pyc

# Plots regenerated on every run of tools/plot_run.py. The archived run CSVs in
# runs/ ARE tracked: they are the record of what each gain setting actually did,
# and they are small.
runs/*.png
